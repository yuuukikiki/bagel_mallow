<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bagel Mallow</title>
    <meta name="description" content="自家製酵母のパンとベーグルのお店">
    <%= csrf_meta_tags %>
    <link href="//fonts.googleapis.com/css?family=Open+Sans+Condensed:300" rel="stylesheet">
    <%= javascript_importmap_tags %>
</head>
<body>
    <header>
        <div class="header-inner">
            <div class="site-logo">
                <a href="<%= root_path %>">
                   <%= image_tag '436410747_1389259099134655_8490224785995163075_n.jpg', alt: 'Bagel Mallow' %>
                </a>
            </div>
            <nav>
                <ul class="main-nav">
                    <li><a href="<%= root_path %>">トップ</a></li>
                    <li><a href="<%= orders_path %>">メニュー</a></li>
                    
                    <% if user_signed_in? %>
                        <li><%= link_to current_user.name, root_path, class: "user-name" %></li>
                        <li><%= link_to 'ログアウト', destroy_user_session_path,data: { turbo_method: :delete }, class: "logout" %></li>     
                    <% else %>
                        <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
                        <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
                    <% end %>
                    
                    <li><a href="<%= new_contact_path %>">お問い合わせ</a></li>
                    <li><%= link_to 'カート', cart_path, class: 'cart-link' %></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <%= yield %>
    </main>

    <footer>
        <div class="footer-inner">
            <div class="footer-copyright">
                ©2024 <a href="<%= root_path %>">Bagel Mallow</a>. All Rights Reserved.
            </div>
        </div>
    </footer>
</body>
</html>