<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>注文 - Bagel Mallow</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
</head>
<body>
  <header>
    <h1><a href="/">Bagel Mallow</a></h1>
    <nav>
      <ul>
        <li><a href="/">トップ</a></li>
        <li><a href="/orders">メニュー</a></li>
        <li><a href="/orders/new">注文</a></li>
        <li><a href="/contact">お問い合わせ</a></li>
      </ul>
    </nav>
  </header>

  <main>
    <h2>注文フォーム</h2>
    <%= form_with model: @order, url: orders_path, local: true do |f| %>
      <div class="form-group">
        <%= f.label :item_id, '商品を選択:' %>
        <%= f.collection_select :item_id, Item.all, :id, :name, prompt: '商品を選んでください' %>
      </div>

      <div class="form-group">
        <%= f.label :quantity, '数量:' %>
        <%= f.number_field :quantity, min: 1, required: true %>
      </div>

      <div class="form-group">
        <%= f.label :name, 'お名前:' %>
        <%= f.text_field :name, required: true %>
      </div>

      <div class="form-group">
        <%= f.label :email, 'メールアドレス:' %>
        <%= f.email_field :email, required: true %>
      </div>

      <div class="form-group">
        <%= f.label :phone_number, '電話番号:' %>
        <%= f.text_field :phone_number, required: true %>
      </div>

      <div class="form-group">
        <%= f.label :shipping_address, '配送先住所:' %>
        <%= f.text_area :shipping_address, rows: 4, required: true %>
      </div>

      <div class="form-group">
        <%= f.submit '注文する', class: 'submit-btn' %>
      </div>
    <% end %>
  </main>

  <footer>
    <p>©2024 Bagel Mallow. All Rights Reserved.</p>
  </footer>
</body>
</html>