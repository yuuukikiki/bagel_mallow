<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>メニュー - Bagel Mallow</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
</head>
<body>
  <header>
    <h1>Mallow - メニュー</h1>
  </header>

  <main>
    <h2>Bagels</h2>
    <ul class="menu-list">
      <% @items.each do |item| %> <!-- 動的にアイテムを表示 -->
        <li class="menu-item">
          <%= image_tag item.image_url, alt: item.name, class: 'menu-image' %> <!-- 商品画像 -->
          <div class="item-details">
            <h3><%= item.name %></h3> <!-- 商品名 -->
            <p><%= item.description %></p> <!-- 商品説明 -->
            <span class="price"><%= number_to_currency(item.price) %></span> <!-- 価格 -->

            <div class="quantity-controls">
              <button class="decrement" data-item-id="<%= item.id %>">-</button>
              <input type="number" value="0" min="0" data-item-id="<%= item.id %>" class="quantity-field">
              <button class="increment" data-item-id="<%= item.id %>">+</button>
            </div>
            <button class="add-to-cart" data-item-id="<%= item.id %>">カートに追加</button> <!-- カートに追加ボタン -->
          </div>
        </li>
      <% end %>
    </ul>
  </main>

  <footer>
    <p>©2024 Bagel Mallow. All Rights Reserved.</p>
  </footer>
</body>
</html>